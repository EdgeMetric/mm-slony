REPLICATIONDB=<name of the database to replicate>
REPLICATIONSCHEMA=('public','<custom schema>')
TABLEOWNER=<role which owns the table>

CLUSTER=<cluster name>

MASTERHOST=<ip address of the master cluster>
MASTERPORT=5432
MASTERUSER=<role with super user access in master cluster>
MASTERPWD=<password for the master super user access>

SLAVEHOST=<ip address of the slave cluster>
SLAVEPORT=5432
SLAVEUSER=<role with super user access in slave cluster>
SLAVEPWD=<password for the slave super user access>